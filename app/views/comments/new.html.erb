<%= form_for :comment, url: post_comments_path do |f| %>
  <div class="field">
    <%= f.label :content %><br />
    <%= f.text_area :content %>
  </div>

  <div class="actions">
    <%= f.submit "Post!" %>
  </div>
<% end %>
